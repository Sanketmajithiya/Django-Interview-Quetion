1.st create repository (git nam)
2.  This pic(Dekstope) open  D drive  Than open CMD
3.  in CmD HTTPS link :- git clone  https://github.com/Sanketmajithiya/Django-Interview-Quetion.git 
4.   New folder ban jaayega D drive me  repository ke name se
5. woh folder ko open karna :- cd D> hoga woh folder me Ajana hai Tab kar ke Syntax:- D>(folder name) cd Tab karenge to bhi ajayega folder name
# Commonds
Ex:- D:\Django-Interview-Quetion>git add .
2. git commit -m "#"
3. git push

#---------------------#----------------------#------------------------#----------------
# create repository
# create folder Dekstope or Open this file on Dekstope 
# cmd open *(check kar lena file ke under hi open kiya haina cmd bhar to nahi kiya naa)

# Commonds

# 1. git init 
# 2. git add .
# 3. git status(Green me add hojayega)
# 4. git commit -m "first commit"
# 5. git remote add origin https://github.com/ Sanketmajithiya/OCTA.NET-ALL-T
# ASK.git(ye joo link hai woh apni repository ki )
# 6. git push origin master

# Youtube 4.34 sec(how to push code to gitHub) Technical voice

# # change branch
# *Ager Branch change karni hai Master to main 

# 1. git init
# 2. git add .
# 3. git status
# 4. git commit -m "#"
# 5. git branch -m master main
# 6. git push --set-upstream origin main
# 7. git pull origin main --rebase
# 8. git status 
# output :-On branch main
# 9. git push origin main (stap check also chatGPT)


# upgrade python version 3.8 to 3.12 in Any python3 or python 
# 1.)  sudo apt update

#  2.) sudo apt install python3.12   <----- Name Any version install you

# 3.) sudo update-alternatives --config python  <------- chose the number which version you want install
#  python --version
#   or
#  python3 --version   output:- 3.12.3


1. Option 1: Safe Merge (Recommended for Teams)
Agar aap chahte hain ki GitHub par jo extra files hain wo delete na hon, balki wo aapke local computer par aa jayen aur mix ho jayen:

bash
git pull --rebase origin main ****
git push origin main
Kaam: Ye pehle GitHub se naye changes layega (pull), unko aapke changes ke neeche lagayega (rebase), aur fir sab kuch wapas bhejega (push).
Problem: Kabhi-kabhi isme "Conflict" aa jata hai agar same line par alag-alag changes hon.
2. Option 2: Force Push (Winner for You 🏆)
Kyunki aapne kaha ki aapne code pura set kar liya hai aur aapka local code hi final hai, to ye command GitHub ko force karegi ki "Mera local code hi sahi hai, baaki sab bhul jao".

bash
git push -f origin main   *****
Kaam: Ye GitHub ki purani history ko overwrite kar dega aur aapke local folder ko exact copy bana dega wahan.
Fayda: Koi conflict nahi aayega, sab kuch clean rahega. medical_chromadb, 
README.md
 sab wahi hoga jo aapke paas abhi hai.
